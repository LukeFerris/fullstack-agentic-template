{
  "name": "fullstack-template",
  "version": "1.0.0",
  "private": true,
  "description": "Fullstack TypeScript template with React frontend and AWS Lambda backend",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "husky",
    "check-staged-coverage": "node scripts/check-staged-coverage.mjs",
    "test": "vitest",
    "test:run": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage",
    "build": "yarn workspace frontend build && yarn workspace backend build && yarn workspace admin-backend build && yarn workspace mission-control-frontend build",
    "lint": "eslint . --ext .ts,.tsx",
    "type-check": "tsc --noEmit",
    "dev": "yarn workspace frontend dev"
  },
  "devDependencies": {
    "@eslint/js": "*",
    "@secretlint/secretlint-rule-preset-recommend": "*",
    "@types/node": "*",
    "@typescript-eslint/eslint-plugin": "^8.56.0",
    "@typescript-eslint/parser": "^8.56.0",
    "@vitest/coverage-v8": "*",
    "eslint": "*",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-jsdoc": "^62.7.0",
    "eslint-plugin-security": "^4.0.0",
    "eslint-plugin-sonarjs": "^4.0.0",
    "globals": "*",
    "husky": "*",
    "lint-staged": "*",
    "rollup": "^4.59.0",
    "secretlint": "*",
    "typescript": "*",
    "typescript-eslint": "^8.56.0",
    "vitest": "*"
  },
  "lint-staged": {
    "package.json": [
      "bash -c 'yarn build'",
      "bash -c 'yarn test:coverage'"
    ],
    "**/*.ts": [
      "npx secretlint",
      "bash scripts/security/check-sast.sh",
      "npx eslint --max-warnings 0 --no-warn-ignored"
    ],
    "**/*.tsx": [
      "npx secretlint",
      "bash scripts/security/check-sast.sh",
      "npx eslint --max-warnings 0 --no-warn-ignored"
    ],
    "**/yarn.lock": [
      "bash scripts/security/check-dependencies.sh"
    ],
    "**/package-lock.json": [
      "bash scripts/security/check-dependencies.sh"
    ]
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "resolutions": {
    "minimatch": "^10.0.0",
    "rollup": "^4.59.0"
  }
}
